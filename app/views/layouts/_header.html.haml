#header
  #logo
    %h1= link_to("Rate Marina's Outfits", '/')
  #membership
    %ul
      - if logged_in?
        %li= link_to('Logout', logout_path)
      - else
        %li= link_to('Login', login_path)
        %li= link_to('Signup', signup_path)
