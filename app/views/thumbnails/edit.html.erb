<h1>Editing thumbnail</h1>

<% form_for(@thumbnail) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.submit "Update" %>
  </p>
<% end %>

<%= link_to 'Show', @thumbnail %> |
<%= link_to 'Back', thumbnails_path %>
